# --- Core API & LLM ---
openai>=1.0.0          # OpenRouter / OpenAI-compatible API calls (embeddings + chat)

# --- Vector search & math ---
faiss-cpu>=1.7.4       # Similarity search over the bug corpus
numpy>=1.24.0          # Embedding matrix ops

# --- Model Context Protocol (MCP) ---
mcp>=0.1.0             # Exposes analyze_bug as an MCP tool

# --- Dashboard ---
streamlit>=1.30.0      # src/dashboard.py inspection UI

# --- Utilities ---
tqdm>=4.65.0           # Progress bars for scripts/build_index.py
python-dotenv>=1.0.0   # Loads OPENROUTER_API_KEY from .env

# --- Tests (optional) ---
pytest>=7.0.0          # tests/test_smoke.py
